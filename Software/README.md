**This folder contains the code to run the Perfect Colour application.**

To run:

`qmake`

`make`

`./AppPerfectColour`

The folder test contains the unit tests that were created for the weight sensor, stepper motor and colour calculations. 


